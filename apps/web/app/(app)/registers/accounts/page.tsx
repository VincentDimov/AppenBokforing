import { AccountsPage } from "@/components/accounts/accounts-page";

/** Supports the public workspace URL requested for the accounts register. */
export default function AccountsRegisterPage() {
  return <AccountsPage />;
}
