import { VoucherListPage } from "@/components/journal-entries/voucher-list-page";

/** Supports the requested application URL without the internal /app prefix. */
export default function PublicVouchersPage() {
  return <VoucherListPage />;
}
